{
  "name": "travelling-media",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix Client\" \"npm run dev --prefix Server\""
  },
  "devDependencies": {
    "concurrent": "^0.3.2",
    "concurrently": "^8.2.0"
  }
}
